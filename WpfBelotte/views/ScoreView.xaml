<UserControl x:Class="WpfBelotte.views.ScoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:WpfBelotte.views"
             mc:Ignorable="d" >
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
        <TextBlock Text="Scores de la partie" FontSize="36" Foreground="White" HorizontalAlignment="Center" Background="Black" Padding="10"/>
        <StackPanel Orientation="Horizontal">
            <view:ScorePairView DataContext="{Binding ScoresPairs}"/>
            <view:ScoreImpairView DataContext="{Binding ScoresImpairs}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
