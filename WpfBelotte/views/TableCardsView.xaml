<UserControl x:Class="WpfBelotte.views.TableCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >

    <StackPanel Orientation="Vertical">
        <ListBox ItemsSource="{Binding ImagesCartes}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding ImageString, Converter={StaticResource ToImage}}" />
                    <DataTemplate.Resources>
                        <Style TargetType="Image">
                            <Setter Property="Height" Value="100"/>
                        </Style>
                    </DataTemplate.Resources>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </StackPanel>
</UserControl>

<!--<Image  Source="/WpfBelotte;component/images/carte_dos.bmp" />-->